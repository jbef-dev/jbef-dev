import { Inter, Jost, Montserrat, Raleway,  } from '@next/font/google';

export const inter = Inter({
  subsets: ['latin'],
  variable: '--font-sans',
  display: 'swap',
});

export const openSans = Jost({
  subsets: ['latin'],
  variable: '--font-logo',
  display: 'swap',
});

export const font_title = Raleway({
  subsets: ['latin'],
  variable: '--font-title',
  display: 'swap',
});
